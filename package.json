{
  "name": "scm-static-site-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with GitHub Pages CI/CD + ESLint quality gate",
  "scripts": {
    "lint": "eslint .",
    "build": "mkdir -p dist && cp index.html dist/ && cp styles.css dist/ && cp script.js dist/ && cp -r assets dist/",
    "start": "npx http-server -c-1 -p 8080"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "@eslint/js": "^9.13.0"
  }
}
